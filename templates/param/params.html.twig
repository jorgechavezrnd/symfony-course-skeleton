{% extends 'base.html.twig' %}

{% block body %}
    <div class="container text-center">
        <div class="row">
            <div class="col">
                {{ name }}
            </div>
        </div>
        <div class="row">
            <div class="col">
                {% if null != email %}
                    {{ email }}
                {% else %}
                    "No existe el email"
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}